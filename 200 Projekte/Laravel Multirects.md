- model muss `redirectTo` mitgeben
```
$model->redirectTo = 'courses.index'
$model->redirectTo = 'courses.show'
```

- collection muss `redirectTo`setzen

Trait `HasMultipleRedirects`ist auf `Model` anwendbar